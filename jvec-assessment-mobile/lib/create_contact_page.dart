import 'package:flutter/material.dart';

class CreateContactPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create Contact'),
      ),
      body: Center(
        child: Text('Create Contact Page'),
      ),
    );
  }
}
