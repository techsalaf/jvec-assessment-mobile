

import 'package:flutter/material.dart';

class UpdateContactScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Update Contact'),
      ),
      body: Center(
        child: Text('Update Contact Form'),
      ),
    );
  }
}
